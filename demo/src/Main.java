import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {


        /*
        System.out.println("Hello world!");
        JFrame frame = new JFrame();
        frame.setTitle("Demo"); // to give title to frame
        frame.setVisible(true); //make frame visible
        frame.setSize(500, 500); // x,y dimension
        //the frame would not close if we simply hit the cross button
        //so to close the frame using x we have to :

        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //frame will exit when x is pressed
        //We can also use( DO_NOTHING_ON_CLOSE ),

        //To prevent the user to resize the frame use:
        frame.setResizable(false);


        //to set logo
//        ImageIcon logo = new ImageIcon("logo.png");
//        frame.setIconImage(logo.getImage());


        //to set background color

        frame.getContentPane().setBackground(Color.orange);
        //custom color
        frame.getContentPane().setBackground(new Color(0,0,0)); //black
        frame.getContentPane().setBackground(new Color(255,0,0)); //red
        frame.getContentPane().setBackground(new Color(0xFFFFF)); //hexadecimal

        */
        MyFrame frame = new MyFrame();


    }

}